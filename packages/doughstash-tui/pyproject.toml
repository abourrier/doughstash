[project]
name = "doughstash-tui"
version = "0.1.0"
description = "Textual TUI for doughstash."
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy>=2.0",
    "textual>=0.80",
]

[tool.uv]
package = true
